<div class="container body-content">





    <form action="/search/results" method="post">
        <h2>Search by:</h2>

        <p>
            @foreach (var column in ViewBag.columns)
            {
                <span>
                    <input type="radio" name="searchType" id="@column.Key" value="@column.Key" checked="@( (ViewBag.searchtype == null) ? column.Key=="all" : column.Key==ViewBag.searchtype )" />

                    <label for="@column.Key">@column.Value</label>
                </span>
            }

        </p>

        <p>
            <label for="searchTerm">Keyword:</label>
            <input type="text" name="searchTerm" id="searchTerm" value="@ViewBag.searchTerm" />
        </p>

        <input type="submit" value="Search" />
    </form>

    <hr />

    <!--If the jobs are null the page only shows for the form. else it will show the list of jobs of categiries right under the form-->
    @if (ViewBag.jobs != null)
    {
        <h3>@ViewBag.title</h3>


        @foreach (var job in ViewBag.jobs)
        {


            <table class="job-listing">
                <!--Job-listing class provided in instruvtions-->

                <tr>
                    <td>
                        <p>Id: @job.Id</p>
                        <p>Name: @job.Name</p>
                        <p>Location: @job.Location</p>
                        <p>Employee: @job.Employer</p>
                        <p>Position Type: @job.PositionType</p>
                        <p>Core Competency: @job.CoreCompetency</p>

                    </td>

                </tr>
            </table>
        }

    }

   
</div>